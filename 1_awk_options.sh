#!/bin/bash
# schema of awk(awk的使用模板):
# awk options program file
# options of awk:
# -F fs To specify a file separator(指定一个文件分隔符)
# -f file To specify a file that contains awk script(指定一个包含awk脚本的文件)
# -v var=value To declare a variable(声明一个变量)